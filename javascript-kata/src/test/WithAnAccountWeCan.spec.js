var Account = require('../main/Account');
var Money = require('../main/Money');

test("deposit an amount to increase the balance", () => {
    expect("no implementation you need to write the test first").toEqual("an implemented test");
});
